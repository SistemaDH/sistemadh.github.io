import fs from'node:fs';import path from'node:path';import{fileURLToPath}from'node:url';
const raiz=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'),dados=JSON.parse(fs.readFileSync(path.join(raiz,'data/transformacoes.json'),'utf8'));
const mapa=Object.fromEntries(dados.transformacoes.map(t=>[t.id,{nome:t.nome,nomeIngles:t.nomeIngles,descricao:t.descricao,caracteristicas:t.caracteristicas,perguntas:t.perguntas}]));
const out=`/** GERADO por tools/gerar-45-transformacoes.mjs. NÃO edite à mão. */
const TRANSFORMACOES = ${JSON.stringify(mapa,null,2)};
function normalizarTransformacao_(valor){const alvo=chaveTexto_(valor&&typeof valor==='object'?(valor.id||valor.nome):valor);if(!alvo)return null;const ids=Object.keys(TRANSFORMACOES);for(let i=0;i<ids.length;i++){const t=TRANSFORMACOES[ids[i]];if(chaveTexto_(ids[i])===alvo||chaveTexto_(t.nome)===alvo||chaveTexto_(t.nomeIngles)===alvo)return ids[i]}return null}
function validarTransformacaoDaFicha_(ficha){let controle=(ficha||{}).controleTransformacao,bruto=(ficha||{}).transformacao;if(!controle&&bruto){const legado=normalizarTransformacao_(bruto);if(legado)controle={id:legado,jogadorPodeAlternar:true,ativa:true}}if(!controle){ficha.controleTransformacao=null;ficha.transformacao=null;return []}const idControle=normalizarTransformacao_(controle);if(!idControle){ficha.controleTransformacao=null;ficha.transformacao=null;return ['Transformação concedida desconhecida.']}const ativa=controle.ativa===true;ficha.controleTransformacao={id:idControle,jogadorPodeAlternar:controle.jogadorPodeAlternar===true,ativa:ativa};if(!ativa){ficha.transformacao=null;return []}const id=normalizarTransformacao_(bruto)||idControle,obj=bruto&&typeof bruto==='object'?bruto:{id:id};if(id!==idControle){ficha.transformacao=null;return ['A transformação ativa não corresponde à concedida pelo Mestre.']}const marcadores=Math.max(0,Math.min(6,Math.trunc(Number(obj.marcadores))||0));ficha.transformacao={id:id,marcadores:marcadores,formaDeLobo:id==='lobisomem'&&obj.formaDeLobo===true,escolhas:obj.escolhas&&typeof obj.escolhas==='object'?obj.escolhas:{}};return []}
function caracteristicasDaTransformacao_(ficha){const id=normalizarTransformacao_((ficha||{}).transformacao);const t=id&&TRANSFORMACOES[id];return t?(t.caracteristicas||[]).map(function(c){return {nome:c.nome,origem:'transformação',texto:c.texto}}):[]}
function dadosDaTransformacao_(ficha){const id=normalizarTransformacao_((ficha||{}).transformacao),t=id&&TRANSFORMACOES[id];if(!t)return null;const marcadores=Math.max(0,Number(ficha.transformacao.marcadores)||0);return {id:id,nome:t.nome,descricao:t.descricao,caracteristicas:t.caracteristicas,perguntas:t.perguntas,marcadores:marcadores,formaDeLobo:ficha.transformacao.formaDeLobo===true,bonusGlobal:id==='semideus'?1:0,resistenciaFisica:id==='fantasma',vulnerabilidadeMagica:id==='fantasma',ataqueNatural:id==='vampiro'?{nome:'Presas',alcance:'Corpo a Corpo',dano:'d6 de dano físico usando sua Proficiência'}:null,desvantagemAcaoReacao:id==='vampiro'&&marcadores===0,bonusFormaDeLobo:id==='lobisomem'&&ficha.transformacao.formaDeLobo?{ataque:'1d10',dano:'1d10'}:null,ancestralidadeAssumida:id==='metamorfo'?((ficha.identidade||{}).ancestralidade||null):null,caracteristicaAncestral:id==='metamorfo'?((ficha.transformacao.escolhas||{}).caracteristicaAncestral||null):null,pvPermanentesPerdidos:id==='reanimado'?Math.max(0,Number((ficha.transformacao.escolhas||{}).pvPermanentesPerdidos)||0):0}}
function ajustarTransformacao_(ficha,a){const acao=chaveTexto_((a||{}).acao);if(acao==='adquirir'){const id=normalizarTransformacao_(a.id||a.nome);if(!id)return {erro:'Transformação desconhecida.'};ficha.controleTransformacao={id:id,jogadorPodeAlternar:a.jogadorPodeAlternar!==false,ativa:true};ficha.transformacao={id:id,marcadores:0,formaDeLobo:false,escolhas:{}};if(typeof aplicarDerivados_==='function')aplicarDerivados_(ficha);return {tipo:'transformacao',acao:'adquirir',id:id,nome:TRANSFORMACOES[id].nome}}
if(acao==='remover'){const antes=normalizarTransformacao_((ficha||{}).transformacao)||normalizarTransformacao_((ficha||{}).controleTransformacao);ficha.controleTransformacao=null;ficha.transformacao=null;if(typeof aplicarDerivados_==='function')aplicarDerivados_(ficha);return {tipo:'transformacao',acao:'remover',id:antes}}
if(acao==='ativar-concedida'){const controle=(ficha||{}).controleTransformacao||{},id=normalizarTransformacao_(controle);if(!id)return {erro:'O Mestre ainda não concedeu uma transformação.'};controle.ativa=true;ficha.controleTransformacao=controle;ficha.transformacao={id:id,marcadores:0,formaDeLobo:false,escolhas:{}};if(typeof aplicarDerivados_==='function')aplicarDerivados_(ficha);return {tipo:'transformacao',acao:'ativar-concedida',id:id,nome:TRANSFORMACOES[id].nome}}
if(acao==='desativar-concedida'){const controle=(ficha||{}).controleTransformacao||{},id=normalizarTransformacao_(controle);if(!id)return {erro:'O Mestre ainda não concedeu uma transformação.'};controle.ativa=false;ficha.controleTransformacao=controle;ficha.transformacao=null;if(typeof aplicarDerivados_==='function')aplicarDerivados_(ficha);return {tipo:'transformacao',acao:'desativar-concedida',id:id,nome:TRANSFORMACOES[id].nome}}
const id=normalizarTransformacao_((ficha||{}).transformacao);if(!id)return {erro:'A ficha não possui transformação.'};ficha.transformacao=ficha.transformacao||{id:id,marcadores:0,formaDeLobo:false,escolhas:{}};
if(acao==='entrar-forma-de-lobo'){if(id!=='lobisomem')return {erro:'Apenas Lobisomem possui Forma de Lobo.'};if(a.pvMarcado!==true)return {erro:'Confirme que o personagem marcou ao menos 1 PV.'};const rr=ficha.recursos||{},atual=Math.max(0,Number(rr.estresseMarcado)||0),max=Math.max(0,Number(rr.estresseMaximo)||0);if(!max||atual+1>max)return {erro:'Não sobra Estresse para entrar na Forma de Lobo.'};const r=ajustarRecurso_(ficha,{chave:'estresseMarcado',delta:1});if(r.erro)return r;ficha.transformacao.formaDeLobo=true;return {tipo:'transformacao',acao:acao,custoEstresse:1}}
if(acao==='sair-forma-de-lobo'){ficha.transformacao.formaDeLobo=false;return {tipo:'transformacao',acao:acao}}
if(acao==='alimentar'){if(id!=='vampiro')return {erro:'Apenas Vampiro pode se Alimentar.'};const n=Math.max(1,Math.min(6,Math.trunc(Number(a.pontosDeVida))||0));if(!n)return {erro:'Informe os PV marcados pelo alvo.'};const r=ajustarRecurso_(ficha,{chave:'estresseMarcado',delta:1});if(r.erro)return r;ficha.transformacao.marcadores=Math.min(6,(Number(ficha.transformacao.marcadores)||0)+n);return {tipo:'transformacao',acao:acao,marcadores:ficha.transformacao.marcadores,custoEstresse:1}}
if(acao==='gastar-marcador'){if(id!=='vampiro'||ficha.transformacao.marcadores<1)return {erro:'Não há marcador de Alimentar-se disponível.'};ficha.transformacao.marcadores--;return {tipo:'transformacao',acao:acao,marcadores:ficha.transformacao.marcadores,efeitoManual:'Use d20 como Dado de Medo na próxima jogada de ação.'}}
if(acao==='atravessar-objeto'){if(id!=='fantasma')return {erro:'Apenas Fantasma pode atravessar objetos.'};const rr=ficha.recursos||{},atual=Math.max(0,Number(rr.estresseMarcado)||0),max=Math.max(0,Number(rr.estresseMaximo)||0);if(!max||atual+2>max)return {erro:'Não sobram 2 Estresses para atravessar o objeto.'};const r=ajustarRecurso_(ficha,{chave:'estresseMarcado',delta:2});return r.erro?r:{tipo:'transformacao',acao:acao,custoEstresse:2}}
if(acao==='mudar-forma'){if(id!=='metamorfo')return {erro:'Apenas Metamorfo pode mudar de ancestralidade.'};const anc=typeof normalizarAncestralidade_==='function'?normalizarAncestralidade_(a.ancestralidade):null,def=anc&&ANCESTRALIDADES[anc];if(!def)return {erro:'Ancestralidade desconhecida.'};const alvo=chaveTexto_(a.caracteristica),lista=def.caracteristicas||[];let escolhida=null;for(let i=0;i<lista.length;i++)if(chaveTexto_(lista[i].nome)===alvo)escolhida=lista[i];if(!escolhida)return {erro:'Escolha uma característica da ancestralidade assumida.'};ficha.identidade=ficha.identidade||{};ficha.origem=ficha.origem||{};ficha.identidade.ancestralidade=anc;ficha.origem.ancestralidadeMista=[];ficha.origem.caracteristicasEscolhidas=[];ficha.transformacao.escolhas.caracteristicaAncestral=escolhida.nome;if(typeof aplicarDerivados_==='function')aplicarDerivados_(ficha);return {tipo:'transformacao',acao:acao,ancestralidade:anc,caracteristica:escolhida.nome}}
if(acao==='jogada-com-esperanca'){if(id!=='lobisomem'||ficha.transformacao.formaDeLobo!==true)return {erro:'Forma de Lobo não está ativa.'};const r=ajustarRecurso_(ficha,{chave:'estresseMarcado',delta:1});if(r.erro)return r;const max=Math.max(0,Number((ficha.recursos||{}).estresseMaximo)||0),frenesi=max>0&&Number(ficha.recursos.estresseMarcado)>=max;if(frenesi)ficha.transformacao.formaDeLobo=false;return {tipo:'transformacao',acao:acao,custoEstresse:1,frenesi:frenesi,dadosDeFrenesi:frenesi?Math.max(1,Math.ceil((Number((ficha.identidade||{}).nivel)||1)/3)):0,efeitoManual:frenesi?'Role a quantidade indicada de d20 e cause a soma como dano físico a todas as criaturas em alcance Muito Próximo.':null}}
return {erro:'Ação de transformação desconhecida.'}}
`;
fs.writeFileSync(path.join(raiz,'backend/45_Transformacoes.gs'),out,'utf8');console.log('backend/45_Transformacoes.gs gerado — 6 transformações.');


